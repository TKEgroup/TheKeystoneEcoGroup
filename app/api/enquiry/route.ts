import {NextResponse} from 'next/server';
export const runtime='nodejs';
const allowed=new Set(['Construction','Green Technologies','Sustainable Materials','Developments','Multiple divisions','Not sure yet']);
const text=(v:unknown,max:number)=>typeof v==='string'?v.trim().slice(0,max):'';
export async function POST(request:Request){let raw:unknown;try{raw=await request.json()}catch{return NextResponse.json({error:'Invalid request.'},{status:400})}if(!raw||typeof raw!=='object')return NextResponse.json({error:'Invalid request.'},{status:400});const d=raw as Record<string,unknown>;if(text(d.website,200))return NextResponse.json({ok:true});const name=text(d.name,120),email=text(d.email,160),phone=text(d.phone,40),service=text(d.service,80),location=text(d.location,160),budget=text(d.budget,80),timescale=text(d.timescale,80),details=text(d.details,4000);if(!name||!email||!service||!location||!details||!allowed.has(service)||!/^\S+@\S+\.\S+$/.test(email))return NextResponse.json({error:'Please complete the required fields.'},{status:400});if(!process.env.RESEND_API_KEY||!process.env.KEYSTONE_ENQUIRY_FROM)return NextResponse.json({error:'Direct submissions are not configured yet. Please use the email option.',code:'EMAIL_NOT_CONFIGURED'},{status:503});const lines=[['Name',name],['Email',email],['Telephone',phone||'Not provided'],['Division',service],['Location',location],['Budget',budget||'To discuss'],['Timescale',timescale||'To discuss'],['Project',details]];const escape=(s:string)=>s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]||c));try{const result=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:'Bearer '+process.env.RESEND_API_KEY,'Content-Type':'application/json'},body:JSON.stringify({from:process.env.KEYSTONE_ENQUIRY_FROM,to:['info@thekeystoneecogroup.co.uk'],reply_to:email,subject:'Website enquiry: '+service,html:'<h2>New Keystone website enquiry</h2>'+lines.map(([k,v])=>'<p><strong>'+escape(k)+':</strong><br/>'+escape(v).replace(/\n/g,'<br/>')+'</p>').join('')})});if(!result.ok){console.error('Email provider rejected enquiry:',result.status);return NextResponse.json({error:'Your enquiry could not be sent. Please use the email option.'},{status:502})}return NextResponse.json({ok:true})}catch{console.error('Email provider unavailable');return NextResponse.json({error:'Your enquiry could not be sent. Please use the email option.'},{status:502})}}